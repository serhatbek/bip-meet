{% macro item(img, title, desc, class) %}
  <div class="features-card{%- if class %} {{ class }}{%- endif %}">
    <figure>
      <img src="{{ img }}" alt="{{ title }}">
    </figure>
    <div>
      <h4>{{ title }}</h4>
      <p>{{ desc }}</p>
    </div>
  </div>
{% endmacro %}